# MySQL / MariaDB (XAMPP) profile
# IntelliJ: Run -> Edit Configurations -> Active profiles: mysql

# MariaDB driver — XAMPP / MariaDB par CLIENT_PLUGIN_AUTH error fix karta hai
spring.datasource.url=jdbc:mariadb://localhost:3306/edupath_db?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
